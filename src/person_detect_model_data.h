#pragma once
#include <cstdint>

// --------- PLACEHOLDER MODEL (invalid!) ---------
// Replace with the real file from the Arduino_TensorFlowLite person_detection example.

extern "C" {
  const unsigned char g_person_detect_model_data[] = { 0x00, 0x00, 0x00, 0x00 };
  const int g_person_detect_model_data_len = sizeof(g_person_detect_model_data);
}
